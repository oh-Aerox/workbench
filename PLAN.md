# 后续计划

实现细节和踩过的坑见 [README.md](README.md)。
本文件记录待办及必要的完成记录；做完一项再勾掉。代码审查的当前证据与严重程度见 [CODE_REVIEW.md](CODE_REVIEW.md) 顶部复核结论。

## 需求达成情况

| # | 需求 | 状态 |
|---|---|:--|
| 1 | agent 工作进度可视化工作台 | ✅ |
| 2 | 只读，不写任何 agent 工作区 | ✅ 三层保证 + 4 条守卫单测 |
| 3 | 支持 claude / codex / workbuddy | ✅ 三家适配器均已验证 |
| 4 | 支持 Win10+ 与 Mac M 芯片 | ✅ 两端均已出包并冒烟通过 |
| 5 | 安装后体积尽可能小 | ✅ Win 1.04 MB / Mac DMG 1.5 MB |
| 6 | 按 agent 划分，允许项目重复 | ✅ 不做跨 agent 合并 |

以上为此前功能与出包验收记录，不代表当前所有问题已关闭。2026-09-17 复核发现的两个 P1（缓存清理、路径白名单）已修复并通过本机测试；仍有 P2/P3 修复待办和跨平台复验，不能统称为增强项。

---

## P0 · 阻塞需求验收

- [x] ~~**macOS (Apple Silicon) 出包**~~ —— 已完成
  - `CI=true npm run tauri build`。**`CI=true` 不能省**：否则 `create-dmg` 的
    Finder 美化步骤会卡到 AppleEvent 超时（-1712）而整个出包失败，详见 README「出包」
  - 产物 `Agent Workbench_0.1.0_aarch64.dmg` 1.5 MB，`.app` 3.5 MB，arm64 单架构
  - `tauri.conf.json` 补了 `bundle.macOS.minimumSystemVersion: "11.0"`
    （Tauri 默认 10.13 是 Intel 时代的值，arm64 本身就要 11 起步）
  - 冒烟：从 DMG 装出来启动正常，进程稳定、LaunchServices 已注册为 GUI 应用
- [x] ~~**Mac 上先验证适配器再出包**~~ —— 已完成，`cargo test` 28 绿 + `scan` 三家全通
  - Codex 不需要微调：本机 `~/.codex/sessions/YYYY/MM/DD/` 结构存在
    （23 个 rollout + 4 个归档），`collect_rollouts()` 原样可用
  - 实机解析：Claude 3 项目 / Codex 9 项目 / WorkBuddy 13 项目，路径全部正确
  - **发现并修掉一个真实 bug**：macOS 上 Claude 把 `/Users/a/x` 编码成 `-Users-a-x`
    （保留前导 `/`），**WorkBuddy 却编码成 `Users-a-x`（吃掉前导 `/`）**。
    原 `decode_project_dir` 只认前导 `-`，后者会掉进 Windows 分支被解成 `Users\a\x`。
    已按 `#[cfg(unix)]` 分平台还原并补单测。
    注：这条兜底路径平时走不到（三家都优先 `peek_cwd` 读会话内容里的真实 cwd），
    本机 25 个项目全部走的 peek，所以 GUI 上看不出来——属于潜伏 bug

## P1 · 优先修复与未验证代码

- [x] **CR-12 · 修正项目白名单的路径语义**（修复前：中）
  - 改为比较真实存在目录的 `canonicalize` 结果；不存在、普通文件或解析失败时拒绝，无小写化或分隔符替换兜底。
  - 本机测试覆盖路径别名、分隔符、实际大小写行为、缺失目录和普通文件；会话内容的信任边界不变。
- [x] **CR-09 · 修正清空缓存及大小显示**（修复前：中）
  - 清空不再主动重新解析；数据查询后更新大小，旧响应不能覆盖新值，读取失败显示“未知”。
  - 后端独占清理等待进行中的查询结束；落盘串行化，删除错误向前端返回。后续新查询仍可重建缓存。
  - 并发等待、清空不重建、查询后通知、大小响应乱序和失败场景均有回归测试。
- [ ] **两个 P1 的跨平台复验**
  - 在 macOS / 大小写敏感卷验证两个真实的同名不同大小写目录互不放行，并检查合法目录别名。
  - 在桌面 GUI 检查首次启动、清空、再次打开项目及文件监听刷新后的缓存大小。当前已通过的是 Windows 单测、前端逻辑测试与构建。

- [x] ~~**验证 Codex `update_plan` 提取**~~ —— Mac 实机数据验证通过
  - `cargo run --example scan -- plans` 提取到 17 份计划，勾选态、条目文字、
    伴随说明（`plan_from_call` 的正文）全部正确，字段形状的猜测是对的
- [ ] **验证 Claude `TodoWrite` 提取** —— 仍然无法验证（快照折叠已按同一口径覆盖它）
  - Mac 上照样没有真实数据：`~/.claude/projects/` 全量 grep `TodoWrite`
    只命中 1 个文件，且是本次会话记录里的字面量，不是真实工具调用（`todos` 字段 0 处）
  - 结论：这条要等到实际用一次带任务清单的 Claude 会话才能验，两台机器都没有样本
- [x] ~~**`update_plan` 快照去重**~~ —— 已实现，见 `adapters::fold_plan_snapshots`
  - 按 `(kind=todos, source)` 折叠，只留 `at` 最大的一条，被折叠的份数记进
    `PlanEntry.revisions`，前端显示成「演进 N 次」角标，信息不丢
  - **`TodoWrite` 走同一条路径**：它的分支结构和 `update_plan` 完全相同，一旦有
    真实数据就是同样的雪崩，所以一并覆盖了，不用等验证
  - 带正文的 `ExitPlanMode` 计划**不折叠**：同一场会话里出现两份通常真的是两份
    不同的计划，合并会丢内容

## P2 · 常规修复与性能优化

- [ ] **CR-16 · 修复默认计划无法收起**（严重程度：中）
  - 内容键已替代数组下标，但空集合同时表示“未操作”和“全部收起”，导致默认卡片持续展开。
  - 区分初始化与用户状态；覆盖单计划点击收起/展开、全部收起、刷新和重排后的状态保持。
- [ ] **CR-11 · 处理 UTF-16 跳过与 GBK 中文乱码**（严重程度：低）
  - 零字节检查先于宽容解码，常见 UTF-16 文件仍被当二进制；GBK 只能保住 ASCII 标记。
  - 覆盖 UTF-16 LE/BE、GBK 中文与真实二进制样本；正确解码或明确报告不支持编码及跳过数量。
- [ ] **CR-07 · 评估 Codex 剩余重复遍历**（严重程度：低；核心 peek 问题已修复）
  - cwd 已按指纹缓存；`parse_project` 仍逐项目收集全部 rollout、重读名称索引。
  - 用多项目、多会话数据测量热缓存扫描，必要时在一次批量查询内共享分组及名称索引，验证统计与归属不变。

## P2 · 数据源扩展

- [x] ~~**源码 `TODO/FIXME` 作为计划面板的补充分区**~~ —— 已实现，见 `repo.rs`
  - **项目计划文档**（本文件这类 `PLAN.md`）自动加载：只走两层目录、毫秒级，
    与 agent 计划合并成一个列表。这才是主角
  - **代码 TODO 标记**手动按钮触发，白名单限制可扫路径，跑在阻塞线程池
  - 实测：eshop 7162 文件首次 85s / 缓存后 1.2s；独立单词规则滤掉 56 处误报
  - **遗留噪声**：eshop 的 296 处里 102 处来自内嵌第三方库
    `inc/class/phpQuery/phpQuery.php`，目录名不在常规忽略列表里，识别不出来。
    可考虑的改进：按「单文件命中数异常高」或「平均行长过长（疑似压缩/生成物）」
    再加一层启发式过滤

## CODE_REVIEW.md 修复进度（2026-09-17 复核）

**撤回此前“17 条全部修复”的记录。** 两个 P1 修复后，当前为 13 项已落实、#7 核心修复后仍有优化、3 项未完成或存在回归；详细对照见 [CODE_REVIEW.md](CODE_REVIEW.md)。

- [x] **CR-01 / CR-02 / CR-03**：running 返回前重算、slug 读侧守卫、坏字节不再截断后续行。
- [x] **CR-04 / CR-05 / CR-06**：快照折叠、Unicode 切片修复、写操作封装与源码扫描单测。Claude 真实 TodoWrite 数据验证仍在 P1 单独跟踪；源码扫描不等同于完备静态分析。
- [x] **CR-08 / CR-10 / CR-14 / CR-15 / CR-17**：重 IO 命令移出主线程、prompt 注入剥离、去抖上限、锁中毒恢复、单次后缀剥离。
- **CR-07**：主要 peek 瓶颈已处理，剩余优化见 P2。
- [x] **CR-09 / CR-12**：缓存清理与真实路径白名单修复，本机回归通过；跨平台复验单独跟踪。
- **CR-11 / CR-13 / CR-16**：未关闭，分别在 P2 / P3 跟踪。

严重程度按当前剩余影响评为 **1 项中（#16）、3 项低（#7、#11、#13）**；P1/P2/P3 是执行顺序，不等同于严重程度。原报告的严重级核心缺陷已有修复。

两个 P1 修复后已实际验证：`cargo test --manifest-path src-tauri/Cargo.toml` **45 项通过**，`node --test tests/cache-ui.test.mjs` **3 项通过**，`npm run build` **通过**。计划面板默认卡片无法收起仍待修复。未重新验证 macOS 出包、大小写敏感卷、完整 GUI、Clippy 或 TOCTOU。

保留此前性能教训：失效源文件检查只在缓存加载时做，避免每个项目 `flush()` 都对全部条目执行 `exists()`。此前记录的 131 ms → 21 s 退化属于历史测量，本次未重测。

## P3 · 低风险加固与功能增强

- [ ] **CR-13 · 处理写入守卫的 TOCTOU 残余风险**（严重程度：低）
  - 断言建目录的副作用已消除；检查与按路径写入/改名/删除之间仍有窗口。
  - 利用需要本地进程能够替换相关目录/链接并命中时机，本次未做攻击复现。
  - 评估约束目录身份与链接解析的文件操作并做平台验证；若选择接受风险，单独记录前提与决定，不标为已修复。

- [ ] **会话级逐轮时间线**
  - P2 阶段的另一个选项（当时选了「成果盘点」）
  - 点开单场会话看逐轮对话、工具调用分布、该场触达的文件清单
- [ ] **导出报告**（markdown / HTML）
- [ ] **多 agent 横向对比**：同一项目被不同 agent 跑过时的用量对比
- [ ] **代码签名**（仅对外分发时需要）
  - Windows：未签名会弹 SmartScreen，需 OV 证书
  - macOS：当前是 adhoc linker-signed（`codesign -dv` 确认），本机自装没问题，
    但拷给别人会被 Gatekeeper 拦；正式分发需 Apple 开发者账号 + 公证

---

## 已知限制（数据源本身的缺失，不是 bug）

这些不在计划内——除非上游 agent 改变记录方式，否则做不到：

- **拿不到逐文件的增删行数**。Claude 只在 `cost-state` 里记会话级总计，
  不记每个文件改了多少行。所以文件排行只能按改动次数排，行数排行只到会话粒度。
  要做到文件级需读 `file-history` 备份文件做 diff，会大幅拖慢并偏离只读轻量的定位
- **Codex 不做文件历史追踪**，成果盘点的文件清单对它恒为空
- **WorkBuddy 没有花费、增删行数、git 分支、计划/待办**，其工具集里就没有这些
- **目录名编码有损**，真实 cwd 一律从会话内容 peek，兜底解码无法还原
  路径里原有的 `-`

## 维护须知

- 改动任何 adapter 的统计口径后，**必须给 `src-tauri/src/index.rs` 的 `VERSION` +1**，
  否则旧缓存会以新字段含义被读出来
- 提交前检查隐私：agent 会在项目目录里留下自有工作目录（`.workbuddy/` 等），
  内含本机用户名，已在 `.gitignore` 挡掉
- 改完 adapter 先跑 `cargo run --example scan` 系列冒烟检查，比开 GUI 快得多
